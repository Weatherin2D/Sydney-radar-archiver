      // Dynamic box height: fixed rows + storm types + hazard section + padding
      const numFixedRows = 32; // + conv mode + dominant storm type
      const stormTypeItemsH = lineHeight + stormTypes.types.length * (lineHeight + 4) + 2;
      const hazardItemsH = hazards.length === 0
        ? lineHeight
        : hazards.length * (lineHeight + 4) + 2;
      const infoBoxHeight = 16 + numFixedRows * lineHeight + stormTypeItemsH + hazardItemsH;